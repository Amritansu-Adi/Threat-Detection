Test fixtures

Run the generator to create lightweight audio/video fixtures used by unit and integration tests.

Commands

PowerShell

```powershell
python tests/fixtures/generate_fixtures.py
```

Requirements

- numpy
- soundfile
- opencv-python

These are listed in `requirements-dev.txt` at the repo root.
